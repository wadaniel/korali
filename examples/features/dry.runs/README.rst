Partial Runs
=====================================================

In this example how the user can use dry runs to initialize a Korali experiment/engine without actually running it.
This can be useful in testing, to make sure that the configuration is correct without the need to perform run of a model that might be too computationally demanding or require a special environment to run.
