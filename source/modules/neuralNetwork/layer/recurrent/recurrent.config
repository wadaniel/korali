{
 "Configuration Settings":
 [
   { 
    "Name": [ "Mode" ],
    "Type": "std::string",
    "Options": [
                { "Value": "GRU", "Description": "Uses the Gated recurrent units (GRUs) architecture." },
                { "Value": "LSTM", "Description": "Uses the Long short-term memory (LSTM) architecture." }
               ],
    "Description": "Specifies the recurrent neural network architecture to use in the layer."
   },
   {
    "Name": [ "Dropout Probability" ],
    "Type": "float",
    "Description": "Specifies the probability with which the value from input is set to zero during the dropout layer."
   }
 ],
 
  "Internal Settings":
 [
 ],
 
 "Module Defaults":
 {
  "Dropout Probability": 0.0
 }
}
