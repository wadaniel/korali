e = find_program('./run-dram.py', required: true)
test('samplers.correctness.dram', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )

e = find_program('./run-hmc-nuts-1.py', required: true)
test('samplers.correctness.hmc-nuts-1', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc-nuts-2.py', required: true)
test('samplers.correctness.hmc-nuts-2', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc-nuts-3.py', required: true)
test('samplers.correctness.hmc-nuts-3', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc-nuts-4.py', required: true)
test('samplers.correctness.hmc-nuts-4', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc-bayesian.py', required: true)
test('samplers.correctness.hmc-bayesian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc.py', required: true)
test('samplers.correctness.hmc', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )        
 
e = find_program('./run-mcmc.py', required: true)
test('samplers.correctness.mcmc', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-tmcmc-1.py', required: true)
test('samplers.correctness.tmcmc-1', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-tmcmc-2.py', required: true)
test('samplers.correctness.tmcmc-2', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )          

e = find_program('./run-tmcmc-3.py', required: true)
test('samplers.correctness.tmcmc-3', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )          
