modules_source = files([
  'module.cpp',
  'module.hpp',
])

subdir('conduit')
# subdir('distribution')
# subdir('experiment')
# subdir('neuralNetwork')
# subdir('problem')
# subdir('solver')

korali_source += modules_source
