e = find_program('./run-hmc-gaussian.py', required: true)
test('samplers.mean.hmc.gaussian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc-laplace.py', required: true)
test('samplers.mean.hmc.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )

e = find_program('./run-hmc-riemannian-laplace.py', required: true)
test('samplers.mean.hmc.riemannian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc-nuts-gaussian.py', required: true)
test('samplers.mean.hmc.nuts.gaussian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-hmc-nuts-laplace.py', required: true)
test('samplers.mean.hmc.nuts.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
 
e = find_program('./run-multinest-gaussian5d.py', required: true)
test('samplers.mean.multinest.gaussian5d', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )   

e = find_program('./run-tmcmc-gaussian5d.py', required: true)
test('samplers.mean.tmcmc.gaussian5d', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )   

e = find_program('./run-mcmc-exponential.py', required: true)
test('samplers.mean.mcmc.exponential', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )   

e = find_program('./run-mcmc-gaussian.py', required: true)
test('samplers.mean.mcmc.gaussian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-mcmc-gaussian5d.py', required: true)
test('samplers.mean.mcmc.gaussian5d', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-mcmc-laplace.py', required: true)
test('samplers.mean.mcmc.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )

e = find_program('./run-nested-gaussian.py', required: true)
test('samplers.mean.nested.gaussian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-nested-laplace.py', required: true)
test('samplers.mean.nested.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-nested-exponential.py', required: true)
test('samplers.mean.nested.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-nested-gaussian5d.py', required: true)
test('samplers.mean.nested.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
          
e = find_program('./run-rchmc-laplace.py', required: true)
test('samplers.mean.rchmc.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )
    
e = find_program('./run-rchmc-gaussian.py', required: true)
test('samplers.mean.rchmc.gaussian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )  
    
e = find_program('./run-tmcmc-gaussian.py', required: true)
test('samplers.mean.tmcmc.gaussian', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )     
    
e = find_program('./run-tmcmc-laplace.py', required: true)
test('samplers.mean.tmcmc.laplace', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    )        
    
e = find_program('./run-tmcmc-exponential.py', required: true)
test('samplers.mean.tmcmc.exponential', e,
      timeout : 2000,
      suite: 'statistical',
      workdir: meson.current_source_dir(),
      depends: python_extension,
      env: nomalloc
    ) 